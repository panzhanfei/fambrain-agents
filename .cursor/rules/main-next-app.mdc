---
description: FamBrain monorepo 结构与 Next 16 App Router 约定
globs: apps/web/**/*.{ts,tsx}
alwaysApply: false
---

# Monorepo 结构

| 路径 | 职责 |
|------|------|
| `apps/web/` | Next.js UI + BFF（`output: standalone`）；`@/*` → `apps/web/src/*` |
| `apps/agents/` | Agent 业务（Intake / KM / Analyst / Indexer CLI） |
| `packages/db/` | Prisma schema、migrations、会话 repo |
| `packages/auth/` | JWT、登录注册、会话（middleware 用 `/constants`、`/jwt` 子路径） |
| `packages/agent-*` | types、config、shared 公共代码 |

**环境变量**：仅仓库根目录 `.env`；`apps/web/next.config.ts` 与 `packages/db/prisma.config.ts` 会加载它。

**数据目录**：语料 `data/doc/users/<userId>/corpus/` · SQLite `packages/db/prisma/dev.db` · Chroma `data/chroma/`

## `apps/web/src` 分层

| 路径 | 职责 |
|------|------|
| `src/app/` | App Router 页面与 `src/app/api` Route Handlers |
| `src/actions/` | Server Actions（`"use server"`） |
| `src/components/` | UI 组件；交互处标 `"use client"` |
| `src/server/` | BFF 编排（如 `handle-post-message`、corpus 解析） |
| `src/lib/` | Web 专属：SSE、security、rate-limit（logout） |
| `src/middleware.ts` | 鉴权拦截（仅 import auth 的 edge-safe 子模块） |

## 依赖约定

- Agent（`@fambrain/agents`）**不**依赖 db/auth
- BFF 负责：读库 → 拼 `pipelineContext` → 调 `runAgentStream` → SSE → 落库
- 构建：`pnpm run build`（根目录）= `db generate` + `next build --webpack`
- 产物：`.next` 在 **`apps/web/.next/`**，不在仓库根

## Next 16 要点

- Server Components 默认；仅交互处 `"use client"`
- 需会话的路由通常 `dynamic`
- 开发：`pnpm run dev`（勿在根目录直接跑 `next`）
